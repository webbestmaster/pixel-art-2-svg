// @flow

/* global describe, it */

import assert from 'assert';
// $FlowFixMe
import pixelArtToSvg from './../dist';

describe('Pixel Art to SVG', () => {
    it('Convert', () => {
        console.log('---> No test ((');
        assert.equal(1 + 1, 2);
    });
});
